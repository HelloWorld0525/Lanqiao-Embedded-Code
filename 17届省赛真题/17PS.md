# 第十七届 蓝桥杯（电子类）嵌入式设计与开发项目 省赛

## 第一部分 客观试题（15 分）

不定项选择，共 `10` 题（`1.5 分/题`）

### 01. 关于 C 语言数组和指针，下列说法正确的是（ ）

- A. 指针的大小与其指向的数据类型相关
- B. 数组名在表达式中通常被转换为指向首元素的指针
- C. 语法上允许指针通过下标运算符来访问它所指向的数组元素
- D. 定义数组时其大小通常在编译时就必须确定

### 02. 下列关于 SPI 通信协议的说法正确的是（ ）

- A. SPI 支持全双工数据传输
- B. SPI 通信必须有独立的时钟线（SCK）
- C. SPI 通过从器件地址（ADDR）区分不同的从设备
- D. SPI 通信必须通过应答信号（ACK）进行确认

### 03. 下列关于流水线技术的说法正确的是（ ）

- A. 流水线提高了指令吞吐量
- B. 减少单条指令的执行时间
- C. 支持多条指令并行执行
- D. ARM Cortex-M4 内核为三级流水线

### 04. 关于一阶 RC 低通滤波器，下列说法正确的是（ ）

- A. 截止频率 `fc = 1 / (2πRC)`
- B. `f = fc` 时，增益降低到 `1/2`
- C. 频率每翻倍，输出幅度下降 `6dB`
- D. 当输入频率远低于 `fc` 时，输出几乎等于输入

### 05. 关于 STM32 内部存储器的说法正确的是（ ）

- A. 内部 Flash 存放程序代码
- B. 内部 Flash 擦写次数有限
- C. 堆和栈均位于内部 SRAM
- D. 内部 SRAM 起始地址为 `0x08000000`

### 06. 下列关于 STM32 内部定时器的说法中正确的是（ ）

- A. 通过主从配置，可以实现定时器级联计数
- B. 高级定时器支持死区时间控制和互补输出
- C. 定时器的时钟频率始终与其所在总线上的时钟频率保持一致
- D. 定时器刹车功能触发后，所有输出通道将被强制清除为低电平

### 07. I2C 总线在多字节连续写模式下以 `400kHz` 时钟，向从机发送 `4KB` 数据，约需（ ）

- A. `82ms`
- B. `92ms`
- C. `102ms`
- D. `112ms`

### 08. 关于移位寄存器，下列说法正确的是（ ）

- A. 移位寄存器是组合逻辑电路
- B. 移位寄存器可以由多个触发器级联构成
- C. 可以通过移位寄存器实现数据延迟
- D. 可以通过移位寄存器实现串并转换

### 09. STM32 系列微控制器中标识 `5V Tolerance` 的 IO 引脚（ ）

- A. 可以输入 `5V`
- B. 可以输出 `5V`
- C. 可以输出 `3.3V`
- D. 需要通过寄存器切换电平兼容模式

### 10. 某场景中，MCU 的 UART（TTL）通信线和电机驱动线长距离并行敷设，通信时偶尔出现乱码、丢帧，有效的处置措施包括（ ）

- A. Tx / Rx 信号线之间并入一个 `120Ω` 终端电阻
- B. Tx / Rx 信号线对地分别配加 `0.1µF` 电容滤波
- C. 协商降低通信波特率
- D. 主从均增加 `RS485` 转换接口
